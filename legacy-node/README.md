# Archived JavaScript implementation

This directory contains the pre-conversion Node.js bot for migration reference only. The repository root .NET solution is authoritative and CI does not execute this runtime.

To inspect the archived tests independently:

```powershell
cd legacy-node
npm ci
npm test
```

Do not place production credentials in this directory or commit a `.env` file.
